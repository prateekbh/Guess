const SET_USER_DETAILS='SET_USER_DETAILS';
function sendUserDetails(data) {
    return dispatch => {
        // TODO send these details to server as well
    }
}
export {
    SET_USER_DETAILS,
    sendUserDetails,
}